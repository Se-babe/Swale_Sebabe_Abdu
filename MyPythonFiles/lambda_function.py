#lambda function 
#what is a lambda function ,use it for small functions ,we use lambda keyword

square = lambda x: x * x
print(square(5))

#add multiple arguments
def add(a, b):
    return a + b

print(add(2, 4))
#use lambda function to get square of list map [1,2,3,4,5]
#numbers[1,2,3,4,5]
# squares=list